# Used Prompts
| Prompt | What I expected | What I got | What I changed |
|---|---|---|---|
| what is the best way to make sure "Time left" will update every second? | I wanted to see if there is an elgant way to update the time left every second | The AI suggested I will rerender the page using 'setNow' (changing a usestate variable) | Added "const [, setNow] = React.useState(Date.now());" and called it every second with setInterval |
| help me think of a different way to make the light and dark mode work because for a split second when loading or reloading the page it shows the default page color which is white so if you refresh while in dark mode it flashes the entire screen white. other than that it works fine | I wanted to see if there is a simple solution to make the page initially load as the current theme color so it want flash every time you reload the page in dark mode | It told me to change document.documentElement in index.html and it helped but caused other problems | after a while of trying to fix bugs caused by the theme I decided to force the theme to be dark for now because it was not asked for anyways |
| why does overFlowY in historyWindow not work? when I inspect element and manually add that it works but for some reason it's not there | When setting up AuctionView it ignored my attempt to make the overflow of the Y axis scrollable | The AI told me that I accidently used the wrong casing | changed "overFlowY" to "overflowY" |
| what is the best way to handle the stream opening and closing? currently I am using new EventSource('http://127.0.0.1:3005/api/stream') to open the connection but for some reason it seems as if it opens 2 event sources (I get 2 connection established logs and 2 error logs when the connection closes). also there is an issue where if the route is down it wont try accessing it when it's up again | I wanted to check if I am using the sse eventsource correctly, it worked but when it couldn't reconnect because the server was down it wouldn't try again | It suggested that ill add a seperate file called "useEventSource.ts" to handle all eventsource uses | Added the file in src/hooks/ |
| why in the updateAuction function auctions is an empty list? | when trying to update specific auctions I tried getting all previous auctions but it returned an empty array | It told me that happens when the updater closes over the value at the time it was created or is called before the fetch finishes | I used the functional state updater (setAuction) using "prev =>" to get the previous auctions |
| I want to also use the sse in AuctionVew and in AuctionListView, how can I do that? | I noticed that useEventSource.ts can only allow one connection at a time in one place only so I wanted to see how can I let both views share the event source | It helped me convert it to useSharedEventSource | Converted useEventSource.ts to useSharedEventSource.ts |
| "Uncaught Error: Too many re-renders. React limits the number of renders to prevent an infinite loop." in AuctionView where is the infinite loop and what causes the many renders? | I wanted to see if there are unnecessary hooks that needed to be removed | The AI sayed it might be the fetchAuctionDetails that I had in the TSX | Moved the fetchAuctionDetails from the TSX to a seperate useEffect that detects when the auction ends ("I probably could have used the auction_ended event instead but wanted to not rely on it incase the connection disconnects exactly when the message is sent") |
|  |  |  |  |